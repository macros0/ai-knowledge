# План доработок OKF Knowledge Service

Дата составления: 17.08.2026 (обновлено: 18.08.2026 — детальный план миграции во внешнюю БД, см. `MIGRATION_PLAN.md`; обновлено: 21.08.2026 — добавлены доработки по мотивации пользователей к разметке глобальных тегов (Этапы 4.1, 4a.1, 5.1); обновлено: 21.08.2026 — добавлены доработки по защите от массовых операций и разделению UI по ролям (Этап 2а), корзина с восстановлением и синхронизация soft delete с Qdrant (Этап 4a.2), дедупликация и near-duplicate detection при загрузке (Этап 4.2))

## Принцип порядка реализации

Пункты сгруппированы не по номерам из исходного запроса, а по зависимостям: часть задач физически невозможна без предыдущей (например, "свои файлы в отдельной области" не имеет смысла без авторизации). Рекомендуемый порядок реализации ниже отличается от порядка нумерации в исходном списке — это сделано намеренно.

**Уточнение от 18.08.2026 (после детального плана миграции, `MIGRATION_PLAN.md`):** схема БД (Этап 2) может быть заложена до фактической реализации авторизации (Этап 1). Таблицы `users`, `roles`, `user_roles`, `organizations` заводятся пустыми, а `documents.owner_id`/`org_id` — nullable. Миграция данных в БД не блокируется отсутствием работающей auth; фактическая авторизация (Этап 1) наполняет уже готовые таблицы. Это снимает жёсткую зависимость «сначала auth, потом БД» на уровне миграции данных, но не отменяет того, что **функционал** Этапов 3–6 остаётся заблокированным до реализации auth в Этапе 1.

**Уточнение от 21.08.2026 (мотивация к тегированию):** отдельного этапа "мотивация пользователей к тегированию" не вводится — механики усиления вовлечённости (авто-подсказки тегов, визуальный статус разметки, замыкание цикла поиск→загрузка) распределены по существующим Этапам 4, 4a и 5, так как они технически являются надстройкой над уже описанными там сущностями (`developments`, тег-виджет, группировка/фильтры), а не новой инфраструктурой.

**Уточнение от 21.08.2026 (защита от массовых операций и дедупликация):** аналогично, защитные механизмы против массового удаления/генерации и алгоритм поиска дублей не выделены в отдельные этапы — они логично добавляются в Этап 2 (rate limiting, роли), Этап 4 (дедупликация при загрузке) и Этап 4a (корзина, синхронизация с Qdrant), поскольку используют уже спроектированные там сущности (`audit_log`, роли, `development_id`, механизм массового редактирования тегов).

---

## Этап 1. Авторизация и роли пользователей

Фундамент — без него невозможно реализовать ни "свои файлы" (Этап 3), ни фильтрацию по автору (Этап 5), ни разграничение доступа к справочнику разработок (Этап 4), ни отдельную роль ИБ для просмотра логов, ни контроль прав на редактирование тегов после загрузки.

**Что сделать:**
- Ввести сущности `User` (id, login, роль) и сессии/JWT для аутентификации.
- Минимум 4 роли: `Viewer`, `Editor`, `Admin`, `Security`.
- Привязать каждый документ к полям `uploaded_by` и `uploaded_at`.
- Роль `Security` проектировать сразу отдельно от `Admin` (segregation of duties).
- Определить, кто вправе редактировать теги уже загруженного документа: только автор (`uploaded_by`) и `Editor`/`Admin`, или любой `Viewer` тоже — это решение нужно принять здесь, так как оно напрямую влияет на реализацию Этапа 4a (редактирование тегов после загрузки).

**Технически:** использовать готовые библиотеки (Passport.js / FastAPI-Users / Django auth) вместо самописной аутентификации.

**Сложность:** средняя. Делать первым, чтобы не переделывать модель данных позже.

---

## Этап 2. Перенос хранения данных во внешнюю БД + журналирование для ИБ

**Детальный план миграции:** `MIGRATION_PLAN.md` (от 18.08.2026). Здесь — верхнеуровневые требования и связь с остальными этапами.

**Хранение данных:**
- Метаданные (документы, теги, пользователи, справочник разработок, статусы OKF-генерации) → реляционная БД (PostgreSQL — разумный выбор, так как дальше нужны JOIN'ы между документами, тегами и справочником разработок).
- Файлы и чанки/эмбеддинги → объектное хранилище (S3-совместимое, MinIO для self-hosted) + при необходимости векторная БД (pgvector как расширение Postgres, либо Qdrant/Milvus) для семантического поиска по чанкам.
- Шифрование at rest и in transit (TLS до БД).
- Резервное копирование данных.

**Уточнения из детального плана (`MIGRATION_PLAN.md`):**
- СУБД: PostgreSQL (prod) + SQLite для dev-режима через тот же SQLAlchemy.
- `documents.json` → таблица `documents`; `tags.json` → `tags` + `document_tags`; `data/staging/{doc_id}/manifest.json` → `document_staging` (JSONB, сырые `chunk_*.md`/`*.json` остаются в FS); `data/okf_bundles/{doc_id}/*.md` → canonical в таблице `okf_concepts`, `.md`-бандлы остаются в FS параллельно как backup/inspect.
- Qdrant payload становится slim (`concept_id` + `doc_id` + `title` + `tags`); полные данные концепта достаются из БД по `concept_id` после поиска.
- Таблицы `users`, `roles`, `user_roles`, `organizations` заводятся **пустыми** в рамках миграции БД (`documents.owner_id`/`org_id` nullable). Фактическая авторизация (Этап 1) наполняет уже готовые таблицы — поэтому миграция данных не блокируется отсутствием auth.
- Промпты (`backend/prompts/*.md`, `data/prompts/*.md`), загруженные бинарники (`data/uploads/`), бинарные вложения бандлов (`data/okf_bundles/{doc_id}/attachments/`) и аварийные дампы LLM (`data/debug/`) — остаются в FS без изменений.

**Журналирование действий (для роли `Security` из Этапа 1):**
- Отдельная таблица/хранилище `audit_log`, физически не пересекающееся с обычными логами приложения (application logs) — журнал ИБ должен фиксировать не отладочную информацию, а именно действия пользователей: вход/выход, загрузка/удаление документа, изменение тегов (в том числе после загрузки, см. Этап 4a), правка справочника разработок, изменения истории чата.
- Минимальный состав записи: `timestamp`, `user_id`, `action_type`, `target_object` (какой документ/чат/справочник затронут), `old_value`/`new_value` (если применимо), `ip_address`.
- Журнал должен быть **неизменяемым** (append-only) даже для роли `Admin` — иначе теряется смысл ИБ-аудита. Технически: либо отдельная БД/схема с правами только на INSERT для сервисного аккаунта приложения, либо write-once хранилище.
- Роль `Security` получает отдельный раздел интерфейса (или отдельное read-only приложение) с фильтрами по пользователю, типу действия и периоду — без доступа к остальному функционалу сервиса.
- Определить и зафиксировать срок хранения журнала ИБ отдельно от срока хранения истории чата (Этап 6) — как правило, требования ИБ к логам аудита строже и предполагают более длительное хранение (например, 1 год и более), чем к пользовательским данным вроде чата.

**Требование к LLM:** если номер/название разработки — потенциально чувствительная информация, извлечение через LLM с титульного листа стоит выполнять локальной моделью (Ollama и т.п.), а не внешним API. Зафиксировать до начала Этапа 4.

**Зависимость:** схема БД и таблицы под auth/organizations заводятся в рамках миграции (Этап 2), но `audit_log` моделируется отдельно — зависит от ИБ-требований к составу записи и сроку хранения. Роль `Security` наполняется в Этапе 1; до этого `audit_log` остаётся пустой таблицей или заведена после фиксации ИБ-требований.

**Сложность:** средняя-высокая, зависит от объёма текущих данных и требуется ли миграция без даунтайма.

---

## Этап 2а. Защита от массовых операций и разделение UI по ролям (добавлено 21.08.2026)

> **Статус (29.08.2026): реализован бэкенд и UI.** Бэкенд-фундамент: `audit_log`
> (append-only), ролевая авторизация `require_role`, bulk-эндпоинты
> (`/documents/bulk-*`), очередь задач `jobs` с four-eyes (второй администратор) и
> circuit breaker, per-user rate limit массовой перегенерации, блоклист
> пользователей (`user_blocks`, действие роли Security). См.
> `backend/app/services/{audit,job_queue,rate_limiter,blocklist}.py`,
> `backend/app/api/{jobs,audit,users}.py`, миграцию
> `alembic/versions/b7f2c9d3e4a1_*.py`. UI: role-gated навигация, мультивыбор +
> danger-zone + typed confirmation (порог 5) + предпросмотр масштаба (admin),
> панель «Системные операции» (`/admin`, журнал задач, approve/cancel), панель
> «Аудит» (`/security`, журнал + фильтры + агрегация аномалий + блокировка +
> экспорт CSV). Route-level guard `RequireRole` на `/admin` и `/security` (редирект
> при недостатке роли). См. README («Защита от массовых операций и журнал ИБ»).
> **Вне объёма (отложено):** real-time алерты/дэшборд аномалий на сервере,
> стриминг логов — отдельной итерацией.

Дополнение к Этапу 2 (rate limiting, audit_log) и Этапу 1 (роли) — конкретные механизмы защиты от случайного или намеренного массового удаления и от перегрузки очереди LLM-запросов массовой генерацией/перегенерацией OKF-концептов. Три разных набора UI-элементов для трёх ролей, а не один экран с переключаемой видимостью кнопок.

### UI пользователя (Viewer/Editor)

- **Опасные операции визуально отделены от обычных.** Кнопки удаления и перегенерации — не рядом с кнопками фильтрации/поиска, а в отдельной секции "Действия с выбранными" с явным разделителем и предупреждающей иконкой.
- **Typed confirmation при массовом удалении.** Единичное удаление — обычный confirm-диалог. При выборе больше 3-5 документов — модалка, где нужно напечатать слово "удалить" или точное число документов, прежде чем кнопка подтверждения активируется.
- **Soft-лимит на массовую перегенерацию OKF на пользователя.** Лимит N документов на одну массовую операцию (например 10-20) и лимит по токенам/запросам в час на пользователя, независимый от общесистемного лимита — защищает и от намеренного, и от случайного залпа LLM-запросов.
- **Обязательный предпросмотр масштаба операции перед подтверждением.** При выборе нескольких документов для удаления/перегенерации — показывать оценку "затронет ~N документов, ожидаемое время ~M минут" до запуска, а не после.

### UI администратора

- **Отдельная панель "Системные операции" вне общего списка документов**, с собственным журналом запущенных задач и статусами ("выполняется", "завершено", "отменено").
- **Rate-limit выше пользовательского, но не отсутствующий.** Массовые задачи Admin ставятся в общую очередь заданий (job queue) с приоритетами, а не выполняются синхронно — обычные пользовательские запросы к чату получают приоритет выше фоновой массовой перегенерации.
- **Двухфакторное подтверждение или четырёх-глазый принцип для операций выше порога** (например, удаление/перегенерация больше 50 документов, изменение прав больше 10 пользователей) — повторный ввод пароля/2FA либо подтверждение вторым администратором.
- **Режим предпросмотра перед запуском.** Кнопка "Показать, что будет затронуто" выводит точный список без выполнения действия — кнопка запуска активна только после просмотра.
- **Circuit breaker на уровне системы.** Если очередь LLM-запросов превышает пороговое значение (например, из-за параллельного запуска нескольких массовых задач разными администраторами), система автоматически приостанавливает приём новых массовых задач и показывает статус "Очередь перегружена, новые массовые операции временно заблокированы".

### UI роли Security

- **Read-only доступ ко всему `audit_log` без исключений**, включая действия самого Admin.
- **Дэшборд аномалий вместо сырого лога** — агрегированные метрики ("пользователь X удалил 340 документов за 10 минут", "начато 5 массовых перегенераций за час"), автоматическое выделение отклонений от обычного паттерна активности.
- **Алерты в реальном времени на пороговые события** — массовое удаление/перегенерация выше порога, изменение прав пользователя.
- **Единственное разрешённое активное действие — временная блокировка пользователя/сессий** для экстренной реакции на подозрительную операцию, без права модификации данных.
- **Экспорт отчётов за период без права модификации** исходных записей `audit_log`.

**Зависимость:** от Этапа 1 (роли), от Этапа 2 (audit_log, требование к LLM).

**Сложность:** средняя — в основном job queue с приоритетами и typed confirmation на фронтенде; circuit breaker и дэшборд аномалий требуют отдельной логики агрегации по `audit_log`.

---

## Этап 3. Разделение интерфейса по пользователям

> **Статус (29.08.2026): реализован.** `GET /api/documents` принимает
> `scope=mine|all`: `mine` фильтрует по `uploaded_by` (username из сессии),
> `all` возвращает полный список. Дефолт по роли: editor/admin — `mine`, иначе —
> `all` (viewer/security/аноним своих документов не заводят, всегда видят общий
> список). `DocumentOut` и `DocumentRegistry.list()` возвращают `uploaded_by`.
> UI: вкладки «Мои документы»/«Все документы» в списке — только для editor/admin,
> в «Все» показывается автор загрузки. См. `backend/tests/test_my_documents.py`.

**Что сделать:**
- В списке документов — вкладки/фильтр "Мои документы" / "Все документы" (доступность второй вкладки — по роли из Этапа 1).
- Реализуется как фильтр `uploaded_by = current_user.id`, без необходимости в отдельном "окне" как физическом элементе интерфейса.
- Для Editor/Admin — возможность видеть, кто ещё что загрузил (полезно при совместной работе над одной разработкой): "мои" как вид по умолчанию, с переключением на общий список.

**Зависимость:** напрямую от Этапа 1 (поле `uploaded_by`).

**Сложность:** низкая, если Этап 1 готов — в основном фронтенд-задача.

---

## Этап 4. Справочник номеров разработки

> **Статус (31.08.2026): реализовано ядро + п. 4.2 (дедупликация) + п. 4a.2 (корзина). Не реализовано:**
> - **4.1** — реализован в рамках Этапа 5 (прогресс разметки `GET /documents/stats` +
>   плашка «Черновик — требует разметки»; см. статус Этапа 5).
> (Сам Этап 4a «редактирование тегов после загрузки» реализован; п. 4a.1 «замыкание
> цикла поиск → загрузка» реализован 31.08.2026 — см. статус в разделе Этапа 4a;
> п. 4a.2 «корзина» реализован 31.08.2026 — см. статус в разделе Этапа 4a.2.)
> Отклонения от плана, зафиксированные при реализации:
> - **`module` — не enum/CHECK, а строковое поле с мягкой валидацией.** Значения
>   живут в generic мини-справочнике `attribute_values` (`attribute_key='module'`,
>   `org_id` nullable — привязка к инсталляции). Значение вне справочника отклоняется
>   422. Сид `PY/PT/OM/PA` — **отдельный скрипт** `backend/scripts/seed_attribute_values.py`
>   (не входит в базовую Alembic-миграцию), привязан к организации инсталляции.
> - **Слой поиска — отдельное payload-поле Qdrant `dev_tags`** (`[number, name, module]`)
>   на всех точках (concept + chunk), а не слияние в `tags`. Поисковый pre-filter
>   матчит тег, если он есть в `tags` **или** `dev_tags`. Переименование разработки =
>   `set_payload(dev_tags=...)` по фильтру `doc_id` без пере-эмбеддинга (`services/dev_sync.py`).
> - **Автоопределение** — regex по имени файла (синхронно на upload) + LLM с титульного
>   листа (в пайплайне, тот же `llm_model`, что и OKF; non-fatal). Кандидат без совпадения
>   кладётся в `documents.development_suggestion` — документ «требует уточнения».
> - **Дедупликация 4.2** — `documents.file_hash` (SHA-256 байтов, блокирующий Level 1 на
>   upload), `content_hash` + `minhash` (k=128, `mmh3`) + `document_lsh_buckets` (двойная
>   banding: strict 8×16 / loose 16×8). Level 1 — блокирующая модалка; Level 2/3 —
>   `GET /documents/{id}/duplicates` (возврат кандидатов с Jaccard; side-by-side merge UI — позже).
> - Миграция: `e3f1a2b4c5d6_stage_4_developments_dedup` (SQLite-портабельна через batch mode).
> - **Семантика синхронизации `dev_tags` с Qdrant.** Связывание документа идёт синхронно
>   (`set_payload` по `doc_id`); при недоступности Qdrant — fallback в однократный фоновый
>   daemon-поток **без ретраев** (`dev_sync.schedule_document_dev_tags_sync`), в ответе
>   приходит одноразовый флаг `dev_tags_sync_pending`. Если Qdrant недоступен в окне потока
>   или процесс рестартовал до его завершения — рассинхрон остаётся, но лечится следующим
>   `_finalize` (regenerate/resume перечитывает `development_id → dev_tags` и переписывает
>   точки). Отдельного персистентного статуса «pending» нет.

### Модель данных: номер разработки как "валидированный тег"

Ключевое архитектурное решение (по итогам обсуждения): номер разработки должен быть одновременно строгой сущностью (для целостности данных и перехода по ссылке на карточку разработки) и обычным тегом (чтобы не ломать существующую систему поиска по тегам). Это решается разведением слоя хранения и слоя поиска:

- **Слой хранения:** таблица `developments` (id, number — уникальный, name, created_at, created_by). В таблице `documents` — поле `development_id` (внешний ключ), `development_confidence` (уверенность автоопределения) и `development_confirmed_by` (кто подтвердил вручную).
- **Слой поиска:** при сохранении документа или изменении связи с разработкой номер и название разработки денормализуются в общий пул тегов документа (обычный `tags` массив либо запись в полнотекстовом/поисковом индексе). Пользователь ищет и вводит номер разработки в поиске так же, как любой другой тег — специальный UI для этого не нужен.
- **Синхронизация:** при переименовании или правке записи в `developments` (например, исправление опечатки в названии) должна запускаться фоновая задача реиндексации всех документов, ссылающихся на эту запись через `development_id` — иначе в поисковом индексе останется устаревшее название как "призрачный" тег. Реализовать через триггер на уровне сервиса (не ручной процесс).
- **Визуальное отличие:** в карточке документа номер разработки отображается как тег с иконкой ссылки, отличным от обычных тегов стилем; клик по нему ведёт на карточку разработки (переход через `development_id`), а не просто фильтрует список по совпадению строки.

### Автоматическое определение номера разработки

Источники, в порядке приоритета:
1. Парсинг имени файла (regex по известным паттернам, например конструкции вида "Пр_10" или похожий шифр в имени) — самый быстрый способ, не требует LLM.
2. Если номер не найден или неоднозначен в имени файла — извлечение LLM с титульного листа (только первая страница документа, JSON-схема с полями `dev_number`/`dev_name`, few-shot примеры на реальных титульниках компании — СЭДО, ЭЛН, инструкции могут иметь разные шаблоны титульного листа).
3. Сопоставление найденного номера/названия со справочником: точное совпадение по номеру, fuzzy-match по названию (LLM может слегка перефразировать название разработки).

### Ручная корректировка номера разработки

- Редактируемое поле "Номер разработки" с автодополнением из справочника.
- Низкая уверенность автоопределения → документ в список "Требует уточнения разработки".

### 4.1 Мотивация пользователя к простановке модуля и номера разработки

Автоопределение (см. выше) снижает трение, но не решает саму мотивацию: пользователь должен *хотеть* подтвердить или исправить тег, а не пропускать шаг. Дополнительно к пп. "Автоматическое определение" и "Ручная корректировка":

- **Предзаполнение как чекбокс на подтверждение, а не готовый факт.** Результат автопарсинга/LLM-извлечения показывается как плашка "Обнаружен модуль PY, разработка 12010 — подтвердить?" с кнопкой в один клик, а не сразу проставляется как финальное значение — пользователь должен явно среагировать, что снижает пропуск ошибок автоопределения и одновременно не требует ручного набора текста.
- **Автокомплит по модулю выводится сразу при выборе номера разработки.** Если номер разработки уже привязан к модулю в справочнике `developments` (после первого документа в этой разработке), модуль подсказывается автоматически и не запрашивается у пользователя повторно для каждого следующего документа той же разработки.
- **Статус "Черновик — требует разметки" как мягкий блок.** Документ можно сохранить без `development_id`/модуля, но в списке документов (Этап 5) он визуально отделяется (например, желтая плашка) от документов со статусом "Готово" — вместо жёсткой валидации на форме загрузки. Это работает как небольшое социальное давление внутри команды без остановки рабочего процесса пользователя.
- **Общий прогресс разметки базы.** Наверху вкладки "Документы" — индикатор вида "78% документов размечены модулем/разработкой", рассчитываемый как доля документов с непустым `development_id` от общего числа. Не требует новых таблиц — агрегирующий запрос по уже существующим полям.

### 4.2 Дедупликация и near-duplicate detection при загрузке (добавлено 21.08.2026)

Проблема: имя файла ненадёжно как идентификатор (пользователь может переименовать копию при сохранении на диск), поэтому нужна проверка по содержимому, а не только по имени. При этом один и тот же документ может встретиться в трёх разных формах, требующих разной реакции UI.

**Три уровня сходства:**

1. **Точное совпадение (байт-в-байт).** SHA-256 хэш всего файла при загрузке, поиск по индексированному полю `documents.file_hash` — сравнение O(1), без квадратичной сложности, независимо от размера базы.
2. **Почти идентичный текст при другом форматировании файла.** Content-hash по нормализованному извлечённому тексту (тот же пайплайн, что уже используется для чанкинга) — та же логика точного хэша, но по тексту, а не по байтам файла.
3. **Содержательно другая версия документа (ревизия).** Точный хэш не подходит — нужен near-duplicate detection через MinHash + LSH:
   - Текст документа разбивается на шинглы (n-граммы), считается MinHash-подпись (k=128 хэш-значений, uint64).
   - Подпись разбивается на b полос по r строк (k = b × r) для построения LSH-индекса: таблица `document_lsh_buckets (document_id, band_index, bucket_hash)` с составным индексом `(band_index, bucket_hash)`.
   - При загрузке нового документа его 32 (или иное число, зависящее от b) пар `(band_index, bucket_hash)` ищутся в этой таблице — это обычный индексированный запрос, возвращающий небольшой список кандидатов, а не перебор всей базы.
   - Для найденных кандидатов считается точный Jaccard similarity по полным MinHash-подписям — только на этом маленьком множестве, что и снимает проблему масштабируемости при росте базы документов.
   - **Выбор порога через формулу `threshold ≈ (1/b)^(1/r)`:** используются **два независимых набора LSH-бакетов** над одной и той же подписью — `strict` (например b=8, r=16 → порог ~0.88, для сценария "тот же документ, переименован") и `loose` (например b=16, r=8 → порог ~0.71, для сценария "вероятно связанная версия"). Дублирование индекса дешёво по месту (единицы КБ на документ, см. ниже), поэтому предпочтительнее одного компромиссного порога.
   - Реализация — библиотека `datasketch` (Python) для MinHash и LSH-запросов либо собственная таблица `document_lsh_buckets` для предсказуемости в продакшене без зависимости от состояния in-memory индекса между перезапусками сервиса.
   - **Дополнительный сигнал** — совпадение метаданных из имени файла (модуль, разработка, том, тип сообщения, версия `v3.3.0`) наряду с Jaccard similarity повышает уверенность в связи "это следующая версия", даже если текст изменился значительно между версиями.

**Оценка объёма хранения LSH-индекса:** при k=128, b=32 — около 3.9 КБ на документ (сырая подпись ~1 КБ + строки бакетов ~1.6 КБ + индекс ~1.3 КБ), то есть при 50 000 документов — порядка 186 МБ. Это на два порядка меньше, чем хранение самих файлов и векторных эмбеддингов чанков в Qdrant — отдельно бюджетировать место под LSH не требуется, достаточно зафиксировать параметры b/r на старте, так как их смена задним числом требует пересчёта подписей для всех документов.

**UI разрешения конфликта — три варианта, не бинарный выбор:**

- **Точное совпадение (Уровень 1).** Модалка: "Этот файл уже загружен как [имя документа] от [дата, кем]. Файл идентичен байт-в-байт." Действия: "Отменить загрузку" или (если существующий документ в корзине, см. Этап 4a.2) "Восстановить существующий вместо повторной загрузки".
- **Почти идентичный текст, разное имя файла (Уровень 2, strict LSH).** Side-by-side сравнение: старое/новое имя, дата и автор существующего документа, разница в размере/числе страниц. Три опции: "Это тот же документ — обновить существующую запись" (сохраняет `development_id`/теги/историю концептов, пересчитывает эмбеддинги), "Это новая версия — сохранить как отдельную ревизию" (соответствует уже используемому в файлах версионированию `v3.3.0`), "Это разные документы — загрузить как отдельный файл".
- **Слабое совпадение (Уровень 3, loose LSH).** Не блокирующее уведомление после успешной загрузки: "Похожие документы, которые могут быть связаны: [список]" — с возможностью вручную связать документы (например, пометить как "предыдущая версия") без остановки загрузки.

**Зависимость:** желательно после Этапа 2 (для хранения `document_lsh_buckets` во внешней БД). П. 4.1 зависит от самого Этапа 4 (наличие `developments`, `development_confidence`) и от Этапа 5 (там же отображается статус и прогресс). П. 4.2 зависит от пайплайна извлечения текста, уже существующего для чанкинга/OKF-генерации.

**Сложность:** средняя. П. 4.1 — низкая, в основном фронтенд поверх уже спроектированных полей. П. 4.2 — средняя: алгоритм стандартный и не требует написания с нуля (готовые библиотеки), но требует продумать пороги под специфику ФС/ТС документов (много однотипных таблиц повышает риск ложных совпадений при слишком низком пороге).

---

## Этап 4a. Редактирование глобальных тегов после загрузки документа

> **Статус (31.08.2026): реализовано.** `PATCH /documents/{id}/tags` (полная замена)
> и `POST /documents/bulk-tags` (delta add/remove) — роли `Editor`/`Admin` на любой
> документ. Синхронизация проекций тегов (`document_tags` → `okf_concepts.tags` →
> frontmatter `.md` → payload Qdrant `tags`); тег-номер разработки — через `dev_sync`.
> Каждая правка — в `audit_log`. Не реализован 4a.2 (корзина/soft delete).
> **4a.1 (замыкание «поиск → загрузка») — реализовано (31.08.2026).** Отклонения от плана:
> - **Условие срабатывания — тег-фильтр + детект, без чипов Этапа 5.1.** В чате нет
>   отдельного фильтра по модулю/разработке; вместо этого детектится, является ли
>   выбранный тег модулем (`attribute_values('module')`) или номером/названием
>   разработки (`developments`). Это соответствует тезису Этапа 4 «номер разработки
>   ищется как обычный тег, спец. UI не нужен» — отдельные чипы (Этап 5.1) не
>   подтягивались.
> - **Пустой результат — короткое замыкание в `/chat`.** При 0 хитах LLM не вызывается,
>   возвращается canned-ответ «Источники не найдены…» + `sources=[]`; фронтенд по
>   пустому `sources` и активному фильтру показывает переход на загрузку.
> - **Префилл — `development_id` прямо в `POST /documents` + `DevelopmentPicker` в
>   панели загрузки.** Привязка при upload важнее автоопределения: `development_id`
>   задаётся до запуска пайплайна, `dev_tags` проставляет `_finalize` (без отдельного
>   реиндекса), `development_confirmed_by` = загрузчик. Передача контекста между
>   вкладками — query-параметры `/?upload_dev=<id>` / `/?upload_module=<модуль>`
>   (одноразовый префилл, после применения URL очищается). Module-only фильтр — только
>   инлайн-хинт «привяжите разработку» (модуль документа задаётся только через
>   разработку).
> **Отклонения от плана, зафиксированные при реализации:**
> - **Qdrant-синк тегов всегда асинхронный** (не только при сбое, как fallback). План
>   предполагал синхронный `set_payload` (по образцу `reindex_document_dev_tags`), но на
>   используемом окружении каждый `set_payload` занимает ~2с, а на документ приходятся
>   десятки concept-точек с разными тегами — синхронное выполнение замораживало HTTP-ответ
>   на десятки секунд. Синк ушёл в фоновый поток, сериализован по документу с dirty-флагом
>   (`document_tag_service.schedule_document_tags_sync`), ответ возвращается мгновенно.
> - **point_id concept-точек — детерминированный `uuid5(filepath бандла)`, без scroll.**
>   Состояние тегов для синка читается из БД (`okf_concepts.tags`), а не из Qdrant — scroll
>   с фильтром по `doc_id` медленный даже без payload. Синк идемпотентен: приводит Qdrant
>   к состоянию БД, порядок правок не важен.
> - **Self-healing ограничен.** Расхождение тегов в Qdrant лечится только явным
>   `regenerate`/`resume` (pipeline перечитывает `doc.tags` как user_tags и переписывает
>   точки); автоматического фонового ретрая нет — это тот же риск «Qdrant недоступен в окне
>   потока», что описан для `dev_tags` в Этапе 4 выше. Отдельного персистентного статуса
>   «pending» для тегов нет.
> - **Frontend**: нативный `<datalist>` заменён кастомным комбобоксом (фильтр по подстроке,
>   счётчики использования, топ-20 совпадений, клавиатура, `aria-activedescendant`), общий
>   словарь тегов `lib/tagDictionary.js` — один `GET /api/tags` на страницу, инвалидация
>   через `bumpTagVersion()` после любой мутации (upload/PATCH/bulk/удаление тегов).
>   Редактор тегов в карточке и панель массовых действий (`SelectionBar`) свёрнуты под
>   спойлеры. Удаление неиспользуемых тегов — `DELETE /tags/{tag}` / `POST /tags/cleanup`.
> - **Технический долг**: фильтры списка документов (включая новый `tag`) живут в локальном
>   React-стейте без синхронизации с URL; Этап 5 (см. выше, «Фильтрация») требует сохранения
>   комбинированных фильтров в query-параметрах для шаринга вида. Не реализовано в этой
>   итерации — будет закрыто при реализации Этапа 5 (панель фильтров мигрирует на URL целиком).

Расширение принципа ручной корректировки (введённого для номера разработки в Этапе 4) на **все** глобальные теги документа, а не только на номер разработки.

**Что сделать:**
- В карточке документа — режим редактирования тегов, доступный в любой момент после загрузки, а не только в момент первичной загрузки: добавление новых тегов, удаление существующих, изменение значения (в том числе для тега номера разработки — переключение документа на другую разработку из справочника).
- Интерфейс — тот же тег-виджет с автодополнением, что и при загрузке: ввод текста показывает существующие теги из общего пула для выбора, чтобы не плодить дубликаты вида "СЭДО" и "сэдо" как разные теги.
- Права на редактирование определяются ролью из Этапа 1: как минимум `Editor` и `Admin`, вопрос про `Viewer` и про ограничение "только автор документа" — зафиксировать явно в момент реализации Этапа 1.
- Любое изменение состава тегов документа обязано фиксироваться в `audit_log` (Этап 2): кто, когда, какой тег добавил/удалил/изменил, к какому документу.
- При изменении тега, который является номером разработки, применяется та же логика синхронизации, что описана в Этапе 4: обновление `development_id`, денормализация в поисковый индекс, фоновая реиндексация — Этап 4a переиспользует механизм из Этапа 4, а не вводит параллельный.
- Отдельно предусмотреть **массовое редактирование тегов**: выбрать несколько документов в списке и добавить/убрать тег сразу у всех, а не по одному (полезно для сценария "загрузил пачку по одной разработке, но не проставил тег сразу").

### 4a.1 Замыкание цикла "поиск → загрузка"

Дополнительный сценарий редактирования тегов, инициируемый не из карточки документа, а из вкладки "Чат": если пользователь на вкладке поиска отфильтровал результаты по модулю и/или номеру разработки и получил пустой результат ("источники не найдены"), показывать прямой переход "Загрузить документ в [модуль] / [разработка]", ведущий на вкладку "Документы" с уже предзаполненным контекстом модуля и `development_id` для новой загрузки. Технически не требует новой сущности — переиспользует автодополнение и предзаполнение полей из Этапа 4/4.1, только с источником предзаполнения "текущий фильтр чата" вместо "результат автопарсинга".

### 4a.2 Корзина: управляемое пользователем восстановление и синхронизация с Qdrant (добавлено 21.08.2026)

Замена первоначальной идеи "toast на 10-15 секунд" на управляемый пользователем раздел "Корзина" — по образцу SharePoint/OneDrive/Canva/ClickUp, где восстановление доступно в любой момент в течение всего окна хранения, а не только в первые секунды после удаления.

**Проблема, которую это решает:** документы проиндексированы не только в Postgres, но и в Qdrant (векторные чанки для RAG-поиска). Если просто выставить `deleted_at` в Postgres, документ пропадёт из списков интерфейса, но продолжит всплывать в ответах чата, потому что Qdrant физически не тронут — это два независимых слоя данных, синхронизация между ними не происходит автоматически.

**Двухслойная модель soft delete:**

- В Postgres: `deleted_at = now()` на записи `documents` — скрывает документ из списков/карточек немедленно.
- В Qdrant: **не** немедленный `Delete Points` (это было бы дорого отменять — требует повторной генерации эмбеддингов), а обновление payload через `Set Payload`: устанавливается поле `deleted: true` на всех точках, связанных с `doc_id`.
- **Обязательный фильтр `must_not: {deleted: true}`** добавляется во все точки входа к Qdrant (основной RAG-поиск из чата, автодополнение по концептам, фоновые задачи реиндексации тегов) — реализуется через единую функцию-обёртку поверх Qdrant-клиента (`search_active()`, `upsert_safe()`), а не рассыпается по каждому месту кода отдельно, чтобы не забыть добавить условие при появлении нового сценария поиска в будущем.
- Восстановление из корзины — снятие обоих флагов (`deleted_at = NULL` в Postgres, `payload.deleted = false` в Qdrant), без пересчёта эмбеддингов, так как точки физически не удалялись из Qdrant всё время нахождения в корзине.
- Физическое `Delete Points` в Qdrant + окончательный `DELETE` из Postgres + удаление файлов из объектного хранилища выполняется фоновой задачей только после истечения единого окна хранения корзины (например 7-14 дней) — это единственный момент, когда операция становится безвозвратной.

**UI корзины:**

- Отдельный раздел "Корзина" на вкладке "Документы" со списком удалённых документов: кто удалил, когда, сколько дней осталось до окончательного удаления (индикация по каждой записи, а не абстрактное правило в документации).
- Toast после удаления — уведомление со ссылкой "Открыть корзину", а не единственный способ отмены с таймером.
- Массовое восстановление из корзины симметрично массовому удалению — выбор нескольких записей и восстановление сразу.
- Видимость корзины по умолчанию — "своя" (симметрично Этапу 3), с расширением до общей корзины для `Editor`/`Admin` — решается тем же способом, что и видимость чужих загрузок.

**Конфликт при восстановлении из корзины:** восстановление документа A прогоняет ту же проверку дедупликации, что описана в Этапе 4.2, против **активных (не удалённых)** документов — если за время нахождения A в корзине кто-то уже загрузил похожий документ B, показывается тот же UI конфликта, что при обычной загрузке ("в системе уже есть [B], похожий на восстанавливаемый [A] — восстановить как отдельный или связать/заменить").

**Зависимость:** от Этапа 1 (права на редактирование/удаление), от Этапа 2 (журнал изменений в `audit_log`, включая действия автоочистки корзины), от Этапа 4 (механизм синхронизации для тега-разработки), от Этапа 4.2 (алгоритм дедупликации, переиспользуемый при конфликте восстановления). П. 4a.1 дополнительно зависит от Этапа 5 (наличие фильтров по модулю/разработке в чате).

**Сложность:** низкая-средняя для 4a — в основном переиспользование существующих компонентов. П. 4a.1 — низкая, фронтенд-переход между вкладками с передачей параметров. П. 4a.2 — средняя: основная сложность не в UI корзины (стандартный паттерн), а в дисциплине применения фильтра `deleted` во всех точках обращения к Qdrant.

> **Статус 4a.2 (31.08.2026): реализовано.** Двухслойный soft delete
> (`documents.deleted_at`/`deleted_by` + payload Qdrant `deleted`), единая обёртка
> `_not_deleted()` для всех путей поиска + DB-side отсечка в `/chat` и `/search`,
> восстановление без пере-эмбеддинга (`POST /documents/{id}/restore`, bulk-restore),
> конфликт дедупликации при восстановлении (409 `code=duplicate`, `?force=true`),
> фоновая автоочистка (`trash_retention_days=14`), UI-раздел «Корзина» с индикацией
> срока и toast «Открыть корзину». Audit: `document_restore`/`document_bulk_restore`/
> `document_auto_delete`. См. `SECURITY.md` §5/§7.

---

## Этап 5. Формы отображения: группировка по тегам/разработкам + фильтры

> **Статус (31.08.2026): реализовано ядро + п. 4.1 (мотивация к разметке) + п. 5.1 (чипы/бейджи). Не реализовано: 5.2a (предупреждение о неточности) и 5.2b (structured-путь) — отложены по решению (см. условие перехода в 5.2b).**
> Реализованные отклонения от плана, зафиксированные при реализации:
> - **Группировка — на клиенте, не `GROUP BY`.** Режим «По тегам» требует
>   multi-membership (документ с несколькими тегами входит в несколько групп), что
>   SQL `GROUP BY` через JOIN размножает в дубликаты строк. Группировка выполняется
>   на клиенте поверх полного отфильтрованного набора (`limit=None`), серверная
>   пагинация в grouped-режиме отключена. Рендер — секции со sticky-заголовками
>   (тег/«номер · название» разработки + счётчик), «Без тега»/«Без разработки» — в
>   конце. Переключатель «Список / По тегам / По разработкам» (`doc-view-mode`).
>   В режиме «По тегам» селект тег-фильтра скрыт (вырожденный случай: группировка
>   по тегам после фильтра по конкретному тегу).
> - **Фильтры**: добавлены статус OKF (Готов / В обработке / Ошибка → `status`,
>   бэкенд уже умел) и диапазон дат загрузки (`date_from`/`date_to`, `created_at`
>   from/to, `date_to` — конец дня в UTC). Все фильтры списка перенесены в
>   query-параметры URL (shareable view): `q,uploader,module,tag,dev,problem,status,
>   from,to,sort,page,group`. Чтение — только при инициализации стейта, запись —
>   `router.replace` (проекция, циклов нет).
> - **4.1 (прогресс + плашка)**: `GET /documents/stats` → `{total, with_development}`
>   по активной базе (`deleted_at IS NULL`), индикатор «N% размечено разработкой»
>   над списком; жёлтая плашка «Черновик — требует разметки» у готовых документов
>   без `development_id` (отличается от «Требует уточнения» при наличии suggestion).
> - **5.1**: фильтр модуля в чате — ряд «последних использованных» чипов (кап 6,
>   персонально, `localStorage`, ключ `okf.recentModules.<username>`, чистая
>   рекуррентность по порядку без таймстемпов) + компактное inline-автодополнение
>   по закрытому справочнику (`ModulePicker.jsx`, поиск не создаёт новых модулей),
>   вместо `<select>`/чипов всех модулей — масштабируется на любое число модулей.
>   Выбор через поиск или чип двигает модуль в начало ряда; взаимоисключение с
>   фильтром разработки сохранено. Бейджи «номер разработки» + «модуль» рядом с
>   каждой цитатой источника (`ChatSource` дополнен `development_number`/
>   `development_name`/`development_module`, обогащение в `chat.py` из `doc_lookup`).
>
**Группировка:**
- Два независимых режима отображения, переключаемых над списком:
  - "По тегам" — группировка по произвольным глобальным тегам (сайдбар с тегами и счётчиками, либо секции со sticky-заголовками).
  - "По разработкам" — группировка строго по `development_id` из Этапа 4, каждая группа — "виртуальная папка" с номером и названием разработки в заголовке.
- Реализовывать как "виртуальные папки" через группировку в запросе к БД (`GROUP BY development_id`), а не как физическое перемещение файлов — документ может участвовать в разных группировках без привязки к одному месту.
- Поскольку номер разработки денормализован и в общий пул тегов (Этап 4), режим "По тегам" тоже покажет номера разработок как один из тегов — это ожидаемо и не противоречит режиму "По разработкам", который использует строгую связь через `development_id` для более надёжной группировки и перехода на карточку разработки.
- Поскольку теги теперь редактируемы после загрузки (Этап 4a), группировка должна пересчитываться динамически при каждом изменении тегов, а не кэшироваться статично на момент загрузки документа.

**Фильтрация:**
- Панель фильтров над списком: автор (Этап 1), диапазон дат загрузки, статус OKF-генерации (Готово / В обработке / Ошибка), номер разработки, произвольные теги.
- Фильтры комбинируются (AND-логика между разными типами) и сохраняются в URL (query-параметры) — удобно делиться отфильтрованным видом с коллегой при совместной работе над разработкой.

### 5.1 Быстрые фильтры модуля в интерфейсе чата

Отдельно от панели фильтров в списке документов, во вкладке "Чат" — постоянно видимые чипы модулей (PY/PT/OM/PA) непосредственно над полем ввода вопроса, а не только в свёрнутом блоке "Параметры поиска". Назначение — не столько ускорить сам поиск, сколько наглядно демонстрировать пользователю ценность тегов в момент, когда он получает выгоду лично для себя: при выборе модуля результат становится точнее, что мотивирует того же пользователя аккуратнее размечать свои документы при следующей загрузке (см. Этап 4.1). Технически использует тот же фильтр по `tags`/`development_id`, что и панель фильтров в списке документов — отдельного индекса не требует.

Дополнительно: в блоке "Источники" под ответом показывать бейджи модуль/разработка рядом с каждой цитатой (`[1]`, `[2]`), используя уже существующие поля `development_id` и `tags` документа-источника — без дополнительных вычислений на стороне поиска.

### 5.2 Structured metadata queries в чате (добавлено 31.08.2026)

Сейчас каждый запрос пользователя в чате проходит единый путь: запрос → векторный поиск (dense/sparse hybrid) в Qdrant → найденный контекст вставляется в промпт → LLM генерирует ответ с ограничениями. Это надёжно для вопросов про содержание документов, но структурно не подходит для списочных и counting-вопросов («какие документы есть с тегом X», «сколько документов по модулю PY», «список разработок без привязанных документов»): LLM в такой схеме видит только top-k чанков, найденных семантическим поиском, а не полный реестр — и не может дать точный список или точное число по построению, независимо от качества промпта с ограничениями.

Решение разбито на два подпункта с разной ценой и разным условием запуска.

#### 5.2a Предупреждение о неточности для списочных запросов (первая очередь)

Не меняет сам RAG-пайплайн — добавляет только индикатор доверия к ответу. Лёгкий классификатор intent (regex/keywords на «список», «какие», «сколько», «все документы» и аналогичные паттерны на русском, без обращения к LLM) проверяет входящий вопрос. Если вопрос помечен как списочный/counting — рядом с ответом в UI выводится предупреждение: «Похоже, вы спрашиваете о списке по критерию. Ответ основан на семантическом поиске и может быть неполным или неточным. Для точного списка используйте раздел "Документы" или "Разработки" с фильтрами.» Запрос всё равно обрабатывается обычным RAG-путём, ответ генерируется как раньше — предупреждение только маркирует его надёжность и направляет пользователя к уже существующим точным инструментам (справочник документов/разработок с поиском и фильтрами, реализованным в Этапе 4).

Объём: один классификатор на чистых правилах + один UI-элемент. Не требует изменений в `DocumentRegistry`, Qdrant или промптах.

Известное ограничение: покрывает только явные формулировки — неявные списочные вопросы («покажи, что у нас есть по HCM») могут пройти без предупреждения. Это принимается как компромисс первой итерации.

#### 5.2b Structured-путь через реестр — точный ответ вместо предупреждения (по необходимости)

Полноценная альтернатива предупреждению: при срабатывании классификатора intent запрос не идёт в векторный поиск, а маршрутизируется к прямому structured-запросу через `DocumentRegistry`/`DevelopmentRegistry` с фильтрацией по `tags`/`dev_tags`/`module`/`development_id` — той же инфраструктурой, что уже реализована для справочника разработок в Этапе 4. Результат (точный список с учётом активных чипов-фильтров из 5.1) передаётся в LLM как готовые данные для оформления ответа, а не как материал для поиска.

Требует решения дополнительных развилок при реализации:
- детекция intent через regex vs LLM function-calling (точность распознавания против стоимости и задержки на каждый запрос);
- смешанные запросы («покажи документы по модулю PY и объясни, что общего между ними») — комбинировать structured-список с RAG или требовать разделения на два сообщения;
- лимит на размер списка в ответе при большом числе совпадений (например, 300 из 1000 документов) — показывать всё, обрезать топ-N с указанием общего количества, или предлагать уточнить фильтр;
- перенос прав доступа (`scope=mine/all`, роли из Этапа 1) в structured-путь — иначе Viewer через чат может получить список документов, к которым у него нет доступа через обычный `GET /documents`;
- поведение при пустом результате фильтра — явный ответ «документов с такими тегами нет», а не сгенерированный LLM правдоподобный, но пустой ответ;
- учёт истории диалога (`chat_messages`, Этап 6) для контекстных уточнений («а по OM?») без явного повторения слова «список»;
- логирование structured-запросов в `audit_log` — при появлении `org_id`-изоляции может понадобиться фиксировать, кто и когда просматривал список чужих документов через чат.

**Условие перехода к реализации:** 5.2a запущен, и накопилась статистика (частота срабатывания предупреждения, обратная связь пользователей), показывающая, что предупреждения недостаточно и списочные вопросы через чат — частый и важный сценарий использования, а не редкий edge case. Если пользователи, получив предупреждение, штатно уходят в раздел "Документы"/"Разработки" за точным списком — 5.2b можно не делать вовсе, оставив предупреждение постоянным решением.

**Зависимость:** от Этапа 4 (`DocumentRegistry`/`DevelopmentRegistry`, фильтрация по `tags`/`module`), от Этапа 5.1 (чипы-фильтры как явные ограничения запроса). 5.2b дополнительно зависит от Этапа 1 (роли/`scope`) и Этапа 6 (история диалога для контекстных уточнений).

**Сложность:** 5.2a — низкая, классификатор на правилах + UI-индикатор. 5.2b — средняя, основная сложность не в самом structured-запросе (переиспользует готовую инфраструктуру), а в перечисленных выше развилках (права доступа, смешанные запросы, лимиты вывода).

**Зависимость:** от Этапов 1, 4, 4a. П. 5.1 дополнительно зависит от Этапа 4 (наличие `development_id`/модуля у документов, участвующих в поиске). П. 5.2a зависит от Этапа 4 и 5.1; п. 5.2b дополнительно от Этапа 1 и Этапа 6.

**Сложность:** средняя, в основном фронтенд + индексы в БД (`development_id`, `uploaded_by`, `uploaded_at`, `tags`). П. 5.1 — низкая, использует существующие данные фильтрации. П. 5.2a — низкая (классификатор на правилах + UI-индикатор). П. 5.2b — средняя, по необходимости (см. условие перехода выше).

---

## Этап 6. История чата: хранение, отображение, автоочистка

Новая сущность с собственным жизненным циклом, отдельная от документов и справочника разработок.

**Хранение:**
- Таблица `chat_messages`: id, user_id, session_id (или thread_id, если пользователь ведёт несколько параллельных диалогов), role (user/assistant), content, created_at.
- Хранить в той же внешней БД (Этап 2), с индексом по `(user_id, created_at)` для выборки истории и последующей очистки по дате.
- Каждый пользователь видит только свою историю (фильтр `user_id = current_user.id`, аналогично Этапу 3); доступ к чужой истории закрыт даже для `Admin`, кроме роли `Security`, если политика ИБ требует возможность расследования инцидентов — согласовать этот момент отдельно, так как переписка может быть чувствительной.

**Отображение:**
- Раздел "История" — список диалогов пользователя по дате, с возможностью открыть/продолжить или начать новый.
- Если чат используется как интерфейс к OKF/поиску по документам — сохранять вместе с сообщением ссылки на документы/чанки, упомянутые в ответе.

**Автоматическое удаление сообщений (3 месяца):**
- Фоновая задача (cron/scheduled job) раз в сутки, удаляющая записи `chat_messages` старше 3 месяцев от `created_at`.
- Через мягкое удаление (soft delete: `deleted_at`) с окончательной физической очисткой через дополнительный отступ (например, ещё 7 дней), а не мгновенный `DELETE` — даёт время на восстановление при ошибке в задаче очистки.
- Показывать пользователю индикацию срока хранения (например, подпись "Сообщения хранятся 3 месяца" при открытии истории).
- Действие автоочистки обязано попадать в журнал ИБ (Этап 2) как системное действие (`action_type = auto_delete`, `user_id = system`).
- Уточнить у ИБ: не противоречит ли 3-месячный срок хранения чата требованиям к журналу аудита, который обычно должен храниться дольше — переписка может удаляться через 3 месяца, но факт обращения к документу через чат может быть обязан остаться в `audit_log` дольше.

**Зависимость:** от Этапа 1 (user_id), от Этапа 2 (внешняя БД, системная запись об удалении в журнал ИБ).

**Сложность:** средняя. Основная нетривиальная часть — согласование, что из чата попадает в постоянный ИБ-журнал независимо от 3-месячной очистки самой переписки.

---

## Этап 7. Многоязычность: управление языками, справочники, поиск и промпты

EN-интерфейс уже добавлен как лёгкий i18n-слой фронтенда: словари живут в `frontend/src/i18n/locales/`, `ru.js` является fallback, `en.js` содержит локализованные ключи, а `index.js` — манифест доступных языков. Новый этап делает многоязычность управляемой через интерфейс администратора, а не через ручные изменения файлов и командную строку. Он охватывает четыре независимых слоя:

1. UI — язык кнопок, форм, уведомлений и страниц.
2. Справочники — теги, разработки и значения атрибутов, отображаемые на языке пользователя.
3. Поиск — language-aware стоп-слова для BM25 и `Matched terms`, чтобы английские служебные слова не создавали ложные hits в русскоязычном SAP/ABAP-корпусе.
4. LLM-промпты — язык исходного документа, язык вопроса и язык ответа для многоязычного корпуса.

Полный текст документов (`okf_concepts.content`) не переводится массово только ради переключения UI: документ остаётся в языке оригинала. Dense-ветка с `bge-m3` используется для cross-lingual retrieval; BM25 остаётся лексической веткой и получает языковые настройки отдельно.

> **Статус: запланировано.** Реализовать до начала production-эксплуатации, пока миграция `tags.name` из текстового PK в surrogate `tag_id` не требует миграции накопленного пользовательского корпуса. Английский UI уже существует; он должен стать первой записью в новом административном контуре управления языками.

### 7.1. Административная вкладка «Поддержка языков»

В панели администратора добавить отдельную вкладку/страницу «Поддержка языков». Она является единым местом настройки языков и замещает ручную поставку файлов в production для штатных операций. Доступ — только `Admin`; действия записываются в существующий append-only `audit_log`.

#### Список языков

Экран показывает таблицу зарегистрированных языков:

| Поле | Смысл |
|---|---|
| Locale | Код языка: `ru`, `en`, `de`, `nl`, при необходимости `pt-BR` |
| Название | Самоназвание и отображаемое название языка |
| Статус | Черновик / активен / отключён |
| UI-переводы | Загружены / неполны / отсутствуют |
| Stopwords | Количество активных слов и дата последнего изменения |
| Теги | Покрытие переводами, число машинных переводов, ожидающих review |
| Разработки и атрибуты | Покрытие переводами, число машинных переводов, ожидающих review |
| Создан / изменён | Кто и когда выполнил операцию |

`ru` остаётся fallback-языком UI. Язык можно подготовить в статусе «Черновик», выполнить загрузку словарей и бэкфилл переводов, проверить поиск, а затем отдельно активировать. Только активные языки доступны в `LocaleToggle` пользователям и участвуют в автогенерации переводов новых тегов.

#### Добавление языка

Администратор создаёт запись языка с locale, названием и статусом «Черновик». Код locale валидируется по BCP 47 / ISO 639; язык не добавляется через изменение идентификаторов тегов, разработок, документов или Qdrant points.

После создания доступны действия:

- «Загрузить UI-перевод».
- «Загрузить stopwords».
- «Открыть редактор переводов и stopwords».
- «Запустить перевод справочников».
- «Проверить готовность».
- «Активировать язык» / «Отключить язык».

Активация разрешена только если UI-словарь прошёл валидацию ключей и загружен хотя бы один набор stopwords. Наличие перевода всех тегов не является жёстким блокером: машинный бэкфилл и последующий review могут продолжаться после запуска языка.

### 7.2. Управление UI-переводами

UI-переводы остаются versioned словарями фронтенда, но источник их обновления переносится в Admin UI.

- Администратор загружает JSON-файл переводов для выбранного locale через «Поддержка языков».
- Backend валидирует JSON до сохранения: допустимая кодировка UTF-8, корректный JSON, отсутствие неизвестных опасных структур, совпадение ключей с базовым `ru.js`/каноническим ключевым набором и корректность параметров интерполяции (`{param}`).
- После валидации словарь сохраняется как runtime override в хранилище данных приложения, а не требует ручного редактирования production-файлов. Правило приоритета: runtime override → versioned словарь релиза → fallback `ru`.
- UI должен уметь получать обновлённый словарь без полного redeploy frontend: через API/кэшированную загрузку словаря с версией. Допускается controlled reload frontend как первый технический шаг, но сама загрузка файла выполняется из Admin UI.
- При замене словаря прежняя версия сохраняется для отката; история загрузок и откатов записывается в `audit_log`.
- Контрольные суммы, манифесты и лицензии как обязательная пользовательская процедура не требуются. Для аудита достаточно записи `audit_log`: locale, пользователь, время, исходное имя файла, размер, версия/ревизия словаря, результат валидации и число ключей.

UI-словарь переводит только системный интерфейс. Он не содержит пользовательские теги, названия разработок и атрибуты — они приходят от backend локализованными через `*_translations`.

### 7.3. Управление stopwords и редактор

Стоп-слова должны храниться как данные, а не как две независимые константы в `services/sparse.py` и `context_builder.py`.

```
locales
───────
code PK
name
status                  -- draft | active | disabled
ui_dictionary_version
created_at
updated_at

stopwords
─────────
locale FK → locales.code
word
created_at
created_by
updated_at
updated_by
PK (locale, word)
```

Единый сервис `services/stopwords.py` отдаёт кэшированный `frozenset` слов для нужного языка. Оба потребителя обязаны использовать только его:

- `services/sparse.py` — токенизация BM25-запроса;
- `services/context_builder.py` — отбор содержательных терминов для `Matched terms` и `drop_unmatched_blocks`.

#### Загрузка stopwords

Во вкладке «Поддержка языков» администратор загружает простой текстовый файл UTF-8: одна запись на строку, пустые строки и строки с `#` игнорируются. Файл может быть получен из NLTK/spaCy, внутреннего словаря или подготовлен вручную — система не требует и не выполняет скачивание из интернета в production.

При загрузке backend:

- нормализует регистр и пробелы;
- валидирует допустимую длину, кодировку и формат;
- удаляет дубли в пределах locale;
- показывает preview: сколько слов будет добавлено, удалено и оставлено без изменений;
- требует явного подтверждения применения изменений;
- заменяет или дополняет набор в одной транзакции;
- инвалидирует кэш `services/stopwords.py`;
- создаёт запись `audit_log` с locale, пользователем, именем файла, числом добавленных/удалённых/итоговых слов и режимом импорта (replace/merge).

Контрольные суммы, манифесты и лицензии не обязательны для штатного UI-процесса. Внешний источник и версия могут быть указаны в необязательном поле «Комментарий к импорту» и сохранены в audit detail.

#### Редактор stopwords

На той же вкладке предоставить редактор:

- поиск и фильтрация слов текущего языка;
- ручное добавление, исправление и удаление слова;
- paste нескольких строк с preview изменений;
- просмотр даты и автора последней правки;
- кнопка «Проверить на тестовых запросах» — запускает/показывает результат заранее настроенного `probe_sources` набора, без изменения Qdrant;
- возможность восстановить предыдущую версию списка из истории импортов, с отдельным подтверждением.

Стоп-слова не должны редактироваться через обычный пользовательский «Справочник тегов»: это отдельный технический справочник внутри «Поддержка языков».

### 7.4. Локализованные теги, разработки и атрибуты

Ключевая правка к текущей модели: `tags.name` не должен оставаться первичным ключом, потому что текст тега — это отображаемая локализуемая строка, а не стабильный идентификатор. Требуется surrogate ID.

```
tags                              tag_translations
─────                              ──────────────────
id PK                              tag_id FK → tags.id
canonical_locale FK → locales.code locale FK → locales.code
merged_into_id NULL FK → tags.id   text
created_by                         is_machine_translated BOOL
created_at                         reviewed_by NULL
                                    translated_at
                                    PK (tag_id, locale)

document_tags                      developments / development_translations
──────────────                     developments.id и number — language-neutral;
doc_id FK                          `number` не переводится;
tag_id FK                           name хранится в translations по locale.
PK(doc_id, tag_id)

attribute_values / attribute_value_translations
код (`PY`, `PT`, `OM`, `PA`) не переводится; локализуется только label/расшифровка.
```

`merged_into_id` — задел для объединения смысловых дублей. Например, `расчёт зарплаты`, `payroll` и `Gehaltsabrechnung` не должны становиться тремя независимыми тегами, если это один смысл; они должны быть переводами одного `tag_id`.

#### Автообогащение при создании

При создании нового тега, разработки или значения атрибута:

1. Система сохраняет текст, введённый пользователем, как перевод в `canonical_locale` с `is_machine_translated=false`.
2. Для всех **активных** locales создаёт недостающие переводы через утверждённый translation provider.
3. Автоматически созданные варианты получают `is_machine_translated=true` и попадают в очередь review.
4. Если provider временно недоступен, создание исходного тега не блокируется: система создаёт оригинал, фиксирует неполное покрытие и ставит задачу/статус для поздней генерации переводов.

Provider выбирается конфигурацией deployment: локальная LLM (предпочтительно для чувствительных названий разработок), разрешённый внешний API или заранее подготовленный файл. Вариант не должен требовать доступа production к публичному интернету.

#### Бэкфилл при добавлении нового языка

Во вкладке «Поддержка языков» администратор запускает действие «Перевести справочники» для выбранного locale.

- Перед запуском UI показывает число тегов, разработок и атрибутов без перевода, provider, модель/режим и предупреждение о том, что созданные переводы будут черновиками.
- Операция создаёт фоновую job, а не блокирует HTTP-запрос.
- Job обрабатывает только отсутствующие переводы и безопасна для повторного запуска.
- Уже подтверждённые человеком строки не перезаписываются.
- Для file-режима администратор загружает подготовленный JSON/CSV в той же форме; backend валидирует соответствие `entity_id`, исходного locale и target locale до импорта.
- Job записывает прогресс, статистику created/skipped/failed/conflicts и ошибки; результат доступен в админ-интерфейсе и `audit_log`.

### 7.5. Review-режим переводов

Review переводов остаётся в уже существующем окне «Справочник тегов», без отдельной новой страницы для основной пользовательской работы. Для удобства вкладка «Поддержка языков» может открывать это окно сразу с предустановленным фильтром нужного locale.

В «Справочнике тегов» добавить:

- фильтр/вкладку «Требует проверки перевода»;
- выбор locale;
- агрегаты: число тегов и переводов, ожидающих review;
- раскрываемую таблицу «язык → перевод → статус → автор/дата проверки»;
- действие «Подтвердить как есть»;
- ручную правку текста;
- массовое подтверждение выбранных переводов;
- действие «предложить объединение» при смысловом дубле.

`Editor` и `Admin` могут править и подтверждать переводы; `Viewer` видит локализованный текст и маркер непроверенного машинного перевода, но не может его изменять. Каждое подтверждение, изменение или объединение фиксируется в `audit_log`.

Review — контроль качества, а не блокирующая стадия. Непроверенный перевод работает для отображения сразу, но имеет явный статус. Приоритет review: часто используемые теги, общие фильтры, названия разработок, SAP HCM/Payroll/ЭЛН/СЭДО и другие корпоративные термины.

### 7.6. Поиск: поведение при разных языках

BM25 не является переводчиком. Он сравнивает лексические токены, поэтому английский запрос обычно не найдёт русские слова только за счёт BM25. Для cross-lingual смысла используется dense-ветка `bge-m3`; задача language-aware stopwords — не дать BM25 создать ложный сигнал и вытеснить dense-hit в RRF.

Диагностированный сценарий: EN-вопрос содержит `the/of/for/from`, а русский SAP/ABAP-корпус включает латинские идентификаторы (`SELECT … FOR ALL ENTRIES`, `IS_CERTIFICATE_OF_DISABILITY`, `SUM_FOR_NON_BUDGET_FUNDS`). Если EN-служебные слова не фильтруются, BM25 ошибочно ранжирует такие кодовые фрагменты как релевантные. С учётом текущего RRF-веса BM25 1.5 против dense 1.0 мусорная лексическая ветка может вытеснить правильный multilingual dense-hit за границу fused top-k.

Языковой stopword-набор устраняет ложные `the/of/for` совпадения в двух местах: BM25 и `Matched terms`/`drop_unmatched_blocks`. Он не переводит английский запрос на русский и не делает BM25 кросс-языковой; он позволяет dense-ветке, уже находящей русский документ по смыслу, не быть вытесненной шумом.

Для полезного кросс-языкового точного поиска отдельно предусмотреть domain glossary/query expansion:

```
ELN → ЭЛН
incapacity for work → нетрудоспособность
electronic certificate of incapacity for work → электронный листок нетрудоспособности
payroll → расчёт заработной платы
```

Это не stopwords. Такие пары являются доменными синонимами и добавляют полезные термины к BM25-запросу или поисковому контексту. Их управление следует разместить в «Поддержке языков» как редактор доменных терминов/синонимов в последующей подзадаче либо реализовать сразу, если потребуется для качества поиска EN→RU.

### 7.7. Адаптация LLM-промптов для многоязычных документов и вопросов

Промпты должны явно различать язык интерфейса, язык документа, язык вопроса и язык ответа. Сейчас промпты загружаются из versioned Markdown с runtime override и mtime-каскадом; это сохраняется, но их правила расширяются.

#### Генерация OKF из документов

В `okf_system.md` / `okf_user.md` / `okf_chunk.md` добавить правила:

- Определяй язык исходного фрагмента автоматически и сохраняй `source_locale`/`content_language` в метаданных документа/концепта, если уверенность достаточна.
- Не переводи и не перефразируй фактическое содержание исходного документа при генерации концептов: имена полей, SAP-коды, XML-элементы, номера разработок, юридические/регламентные формулировки и цитаты должны оставаться в оригинале.
- `title`, `content`, теги и relation-имена генерируй преимущественно на языке исходного документа, если отдельная политика не требует иного.
- Для mixed-language документа допускай `source_locale='mixed'` и не принуждай его к одному языку.
- Если термин известен в доменном глоссарии, при необходимости добавляй переводимый alias/metadata, но не заменяй оригинальный технический идентификатор.

#### RAG-чат

В `chat_system.md` / `chat_user.md` добавить правила:

- Определи язык последнего вопроса пользователя; отвечай на нём по умолчанию.
- Содержимое источников не переводи целиком и не выдумывай перевод. При цитировании сохраняй оригинальные названия, коды, номера и формулировки; при необходимости рядом дай краткое пояснение на языке ответа.
- Если вопрос задан на одном языке, а источник на другом, разрешается сформулировать ответ на языке вопроса, опираясь только на факты источника.
- Не утверждай, что в источнике есть буквальная фраза на языке вопроса, если она существует только как смысловой перевод.
- При недостаточной уверенности в переводе терминов используй оригинал + локализованное объяснение, а не подменяй SAP/корпоративный термин свободным переводом.
- В context metadata передавать `query_locale`, `source_locale`, `ui_locale` и при наличии использованные glossary aliases, чтобы модель не смешивала языки случайно.

#### Извлечение разработки и таблиц

Для `dev_number_system.md` / `dev_number_user.md` и промптов обработки таблиц:

- Извлекай номер разработки независимо от языка титульного листа; номер/код не переводится.
- Название разработки сохраняй в языке первоисточника как canonical translation; дополнительные языки получает общий сервис автоперевода и review, а не LLM-экстракция документа.
- Язык документа не должен ухудшать программную экстракцию таблиц и SAP/XML-идентификаторов.

Изменения промптов принимаются через тот же `probe_sources` + набор multilingual чат-кейсов: RU→RU, EN→RU, RU→EN-документ, EN→mixed-документ, запрос с SAP-кодом/аббревиатурой и запрос с фильтром по локализованному тегу.

### 7.8. API и фоновые операции

Ориентировочные административные API-контракты:

```
GET    /api/admin/locales
POST   /api/admin/locales
PATCH  /api/admin/locales/{locale}
POST   /api/admin/locales/{locale}/activate
POST   /api/admin/locales/{locale}/disable

POST   /api/admin/locales/{locale}/ui-dictionary/validate
POST   /api/admin/locales/{locale}/ui-dictionary/import
GET    /api/admin/locales/{locale}/ui-dictionary/history
POST   /api/admin/locales/{locale}/ui-dictionary/rollback

GET    /api/admin/locales/{locale}/stopwords
POST   /api/admin/locales/{locale}/stopwords/import?mode=replace|merge
PATCH  /api/admin/locales/{locale}/stopwords/{word}
DELETE /api/admin/locales/{locale}/stopwords/{word}
POST   /api/admin/locales/{locale}/stopwords/probe
GET    /api/admin/locales/{locale}/stopwords/history
POST   /api/admin/locales/{locale}/stopwords/rollback

POST   /api/admin/locales/{locale}/translations/backfill
GET    /api/admin/locales/{locale}/translations/status

GET    /api/tags?locale=<locale>&needs_review=true
PATCH  /api/tags/{tag_id}/translations/{locale}
POST   /api/tags/bulk-review
```

Новые операции импорта, rollback и полного backfill являются изменяющими состояние действиями: применить существующую модель Admin-прав, typed confirmation/preview, job queue, rate limit, аудит и системную панель статусов. До подтверждения UI должен показывать фактический объём изменений: число UI-ключей, слов stopwords, тегов/разработок/атрибутов, а также режим replace/merge и provider.

**Зависимость:** от Этапа 1 (роли `Admin`/`Editor`), Этапа 2 и 2а (PostgreSQL, `audit_log`, job queue, подтверждения и защита массовых операций), Этапа 4 (`developments`, `attribute_values`, `dev_tags`), Этапа 4a (существующее окно «Справочник тегов», синхронизация теговых проекций) и существующего UI i18n (RU/EN). Адаптация промптов зависит от существующего механизма runtime/versioned prompt overrides, но может внедряться независимо от миграции `tag_id`.

**Сложность:** средняя-высокая. Основная миграционная сложность — `tags.name → tags.id` и перевод всех связей/проекций (`document_tags`, `okf_concepts.tags`, Qdrant payload) на stable ID; до production это наиболее дешёвый момент для такой правки. Admin UI, загрузка словарей и stopwords, редактор, аудит, preview/rollback и фоновые jobs используют уже реализованные паттерны админ-панели, `audit_log` и job queue. Перевод справочников автоматизирован: человек выполняет review и исправления, но не обязан вручную переводить весь существующий пул тегов. Промптная адаптация технически мала, но требует тщательной multilingual-приёмки на реальном корпусе.

---

## Сводная таблица

| Этап | Содержание | Зависит от | Сложность | Приоритет |
|---|---|---|---|---|
| 1 | Авторизация, роли (включая роль Security) | — (наполняет готовые таблицы из Этапа 2) | Средняя | Критичный |
| 2 | Внешняя БД (ИБ) + журнал аудита + требование к локальной LLM | — (для схемы БД); `audit_log` — от фиксации ИБ-требований | Средняя-высокая | Высокий, можно первым (схема + миграция данных) |
| 2а | Защита от массовых операций + разделение UI по ролям (Admin/Security/User) | Этапы 1, 2 | Средняя | Высокий |
| 3 | Разделение "мои файлы" | Этап 1 | Низкая | Средний |
| 4 | Справочник разработок как валидированный тег + автоопределение | Этап 2 (желательно) | Средняя | Высокий |
| 4.1 | Мотивация к разметке: подтверждение тега, статус "черновик", прогресс-бар | Этап 4, Этап 5 | Низкая | Высокий |
| 4.2 | Дедупликация: точный хэш + MinHash/LSH для похожих документов | Этап 2, пайплайн извлечения текста | Средняя | Высокий |
| 4a | Редактирование глобальных тегов после загрузки | Этапы 1, 2, 4 | Низкая-средняя | Высокий |
| 4a.1 | Замыкание цикла "поиск → загрузка" из чата | Этап 4a, Этап 5 | Низкая | Средний |
| 4a.2 | Корзина с восстановлением + синхронизация soft delete с Qdrant | Этапы 1, 2, 4, 4.2 | Средняя | Высокий |
| 5 | Группировка по тегам/разработкам + фильтры | Этапы 1, 4, 4a | Средняя | Средний |
| 5.1 | Чипы модулей и бейджи источников в чате | Этап 4, Этап 5 | Низкая | Средний |
| 5.2a | Предупреждение о неточности для списочных/counting-запросов в чате | Этап 4, Этап 5.1 | Низкая | Средний |
| 5.2b | Structured-путь через реестр вместо предупреждения (точный ответ) | Этапы 1, 4, 5.1, 6 | Средняя | Низкий (по необходимости) |
| 6 | История чата + автоудаление через 3 месяца | Этапы 1, 2 | Средняя | Средний, после этапа 2 |
| 7 | Многоязычность: управление языками (Admin-вкладка), UI-переводы, stopwords, локализованные теги/разработки/атрибуты (surrogate tag_id + translations), review, language-aware поиск и адаптация LLM-промптов | Этапы 1, 2, 2а, 4, 4a + существующий UI i18n | Средняя-высокая | Средний, после i18n UI; до production |

## Риски и на что обратить внимание отдельно

- **Рассинхронизация тег-индекса при правках справочника.** Без фоновой реиндексации, описанной в Этапе 4, переименование разработки в справочнике оставит в поиске устаревшее название как отдельный "призрачный" тег, не совпадающий с актуальным — обязательно закрыть этот механизм ещё на этапе проектирования, а не добавлять постфактум.
- **Неизменяемость журнала ИБ.** Журнал аудита (`audit_log`) должен быть append-only даже для роли `Admin` — иначе он не выполняет функцию контроля, а лишь имитирует её.
- **Конфликт сроков хранения.** Уточнить у ИБ, не должна ли часть информации о действиях в чате (например, факт запроса к определённому документу) сохраняться в `audit_log` дольше, чем 3 месяца, отведённые на саму переписку — это два разных требования, которые легко перепутать при реализации.
- **Доступ роли Security к истории чата.** Явно согласовать: имеет ли ИБ право просматривать содержимое переписки пользователей при расследовании инцидентов, или только сам факт обращений (метаданные без текста сообщений) — это существенно влияет на модель прав доступа к таблице `chat_messages`.
- **Неконтролируемое редактирование тегов.** Без чёткого ограничения прав из Этапа 1 (кто именно может редактировать теги после загрузки) есть риск, что документ "потеряется" для поиска из-за случайного удаления тега кем-то, кто не является ни автором, ни ответственным за документ — обязательно определить минимальный набор ролей с правом на эту операцию до реализации Этапа 4a.
- **Массовое редактирование тегов и журнал ИБ.** Если операция применяется сразу к пачке документов, в `audit_log` должна фиксироваться отдельная запись на каждый затронутый документ, а не одна общая запись на всю пакетную операцию — иначе аудит потеряет возможность точечно отследить изменение конкретного документа.
- **Риск "разметки для галочки".** Мягкий блок ("Черновик — требует разметки" из п. 4.1) снижает трение, но не гарантирует смысловую точность тега — пользователь может подтвердить некорректно определённый модуль/разработку одним кликом, не проверяя. Стоит периодически выборочно проверять документы с высокой `development_confidence`, но подтверждённые "по умолчанию", на смысловое соответствие — иначе автоматизация из Этапа 4 постепенно снизит, а не повысит качество метаданных.
- **Рассинхронизация Qdrant при soft delete (новый риск).** Если фильтр `must_not: {deleted: true}` не применён во всех точках входа к Qdrant (например, забыт в новом сценарии поиска, добавленном позже), удалённый документ продолжит участвовать в RAG-ответах чата, несмотря на то, что он скрыт из интерфейса списка документов — обязательно использовать единую функцию-обёртку поверх Qdrant-клиента, а не дублировать фильтр в каждом месте кода отдельно.
- **Ложные срабатывания дедупликации на однотипных документах (новый риск).** ФС/ТС-документы часто содержат структурно похожие таблицы (справочники кодов, типовые разделы) даже для разных разработок — слишком низкий порог LSH/Jaccard может создавать ложные предупреждения о дублировании. Требуется откалибровать порог (см. Этап 4.2) на реальном корпусе документов до включения строгого сценария в продакшен, иначе пользователи начнут игнорировать предупреждения о дублях как "шум".
- **Пересчёт LSH-параметров задним числом.** Если после запуска станет ясно, что выбранные b/r дают слишком много false positives/negatives, смена параметров требует пересчёта MinHash-подписей и перестройки таблицы `document_lsh_buckets` для всех существующих документов — это разовая, но не мгновенная фоновая задача, которую нужно закладывать в план обслуживания, а не считать невозможной.
- **Структурное ограничение RAG для counting/списочных вопросов (новый риск).** Векторный поиск возвращает top-k релевантных чанков, а не полный реестр — LLM не может дать точный список/число документов по фильтру, независимо от качества промпта с ограничениями. Требует отдельной маршрутизации к structured-запросу через реестр (см. Этап 5.2), а не решается промпт-инжинирингом. Первая итерация (5.2a) закрывает риск честным предупреждением, а не точным ответом — важно не путать снижение риска с его устранением при коммуникации с пользователями.
- **Миграция PK `tags.name → tags.id` (новый риск, Этап 7).** Переписка `document_tags.tag` (TEXT) на `tag_id` (FK) и переход payload Qdrant с текста тега на id затрагивают все слои дрейка тегов (Этап 4a) — пропуск любого из них даёт «призрачный» фильтр на старый текст. Делать до накопления данных (прод не эксплуатируется); обязателен пересбор payload без пере-эмбеддинга (см. Этап 2b) и сверка `check_integrity`.
- **Качество машинного перевода лейблов (новый риск, Этап 7).** Автоперевод коротких тегов/названий разработок может давать неточные варианты (аббревиатуры, имена собственные). Без review-режима перевод «сразу работает» в поиске и фильтрах и риск тихо закрепляет ошибку — важно, чтобы `is_machine_translated=true` был виден и снимался подтверждением/правкой в том же окне справочника, а не требовал отдельного экрана.
- **Дублирование логики стоп-слов (новый риск, Этап 7).** Два независимых русских списка (`sparse.py`, `context_builder._MARKER_STOPWORDS`) уже разошлись в поведении при добавлении EN-запроса. До выноса в единую `stopwords`-таблицу (и Admin-редактор в «Поддержке языков») любой тактический фикс EN-списка обязан вноситься в оба места синхронно — иначе BM25-фильтрация запроса и анти-шумовой фильтр контекста начнут противоречить друг другу; единый сервис `get_stopwords` снимает этот класс багов навсегда.
- **Активация языка при неполном покрытии (новый риск, Этап 7).** По замыслу активация требует только валидного UI-словаря и хотя бы одного набора stopwords — машинный бэкфилл переводов справочников может продолжаться после запуска языка. Риск в том, что пользователь сразу увидит часть тегов/разработок на языке с непроверенными машинными переводами и доменные термины без review; смягчается явным статусом непроверенного перевода, приоритизацией review по корпоративным терминам (SAP HCM/Payroll/ЭЛН/СЭДО) и предупреждением о черновиках при запуске backfill.
