// Общие константы фронтенда (листовой модуль — без импортов из компонентов,
// чтобы не создавать циклов зависимостей).

// Порог typed confirmation для деструктивных операций над >N объектами (Этап 2а).
// Применяется к массовому удалению документов и к удалению разработки.
export const TYPED_CONFIRM_THRESHOLD = 5;
