// Конфигурация линтера фронтенда. Отдельный файл, а не флаги в CI: локальный
// `npm run lint` и гейт обязаны видеть один и тот же набор правил.
import next from "eslint-config-next";

export default [
  { ignores: [".next/**", "node_modules/**", "public/**"] },
  ...next,
  {
    rules: {
      // Три правила react-hooks срабатывают на КОДЕ, написанном до появления
      // линтера: 19 setState-в-эффекте, обращение к ref во время рендера
      // (OkfFileList) и чтение переменной до объявления (DocumentList).
      // Все три — про паттерны, а не про опечатку: правка каждого меняет
      // порядок рендеров и требует своей проверки в браузере. Держим их
      // предупреждениями, чтобы гейт был зелёным и осмысленным для всего
      // остального, а не выключаем — счётчик должен быть на виду и убывать.
      "react-hooks/set-state-in-effect": "warn",
      "react-hooks/immutability": "warn",
      "react-hooks/refs": "warn",
    },
  },
];
